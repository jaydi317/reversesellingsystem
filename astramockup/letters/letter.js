const h=document.getElementById('calc-hours'),r=document.getElementById('calc-rate'),v=document.getElementById('calc-value');function calc(){v.textContent=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(Math.max(0,Math.min(168,+h.value||0))*Math.max(0,Math.min(10000,+r.value||0))*50)}h.addEventListener('input',calc);r.addEventListener('input',calc);calc();function tick(){const x=new Date('2026-09-10T19:00:00-04:00')-Date.now();document.getElementById('event-clock').textContent=x>0?'The night starts in '+Math.floor(x/86400000)+' days, '+Math.floor(x%86400000/3600000)+' hours.':'September 10, 2026 · Program 7:00 to 9:00 PM';}tick();setInterval(tick,60000);